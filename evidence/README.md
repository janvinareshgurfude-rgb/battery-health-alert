# Evidence Folder

Place your captured evidence here before submitting, for example:

- alert-monitor.log - console output from `docker compose logs alert-monitor` during a full drain-and-recover run
- ditto-thing-before.json / ditto-thing-during-alert.json / ditto-thing-after.json - output of `curl -u ditto:ditto http://localhost:8080/api/2/things/hag:vehicle-01` captured at each stage
- Screenshots of the terminal output showing the batteryAlert transition lines
